'use client';import{useState}from'react';export default function BillingActions({hasCustomer}:{hasCustomer:boolean}){const[busy,setBusy]=useState('');async function go(path:string,body?:any){setBusy(path);const r=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined});const j=await r.json();setBusy('');if(j.url)location.href=j.url;else alert(j.error||'Unable to continue')}return <div className="actions"><button disabled={!!busy} onClick={()=>go('/api/billing/checkout',{plan:'pro'})}>Choose Pro</button><button disabled={!!busy} onClick={()=>go('/api/billing/checkout',{plan:'premium'})}>Choose Premium</button>{hasCustomer&&<button className="secondary" disabled={!!busy} onClick={()=>go('/api/billing/portal')}>Manage billing</button>}</div>}
