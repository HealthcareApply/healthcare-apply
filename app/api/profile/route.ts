import {NextResponse} from 'next/server';import {createClient} from '@/lib/supabase/server';
export async function PUT(req:Request){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const p=await req.json();const row={id:user.id,full_name:p.fullName,headline:p.headline,summary:p.summary,years_experience:Number(p.yearsExperience)||0,skills:p.skills||[],achievements:p.achievements||[],education:p.education||[],certifications:p.certifications||[],target_titles:p.targetTitles||[],preferred_locations:p.preferredLocations||[],remote_preference:p.remotePreference||'any',salary_min:p.salaryMin?Number(p.salaryMin):null,desired_salary_max:p.salaryMax?Number(p.salaryMax):null,industries:p.industries||[],profile_verified:true,onboarding_complete:true,updated_at:new Date().toISOString()};const {error}=await supabase.from('profiles').upsert(row);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true})}
