import './globals.css';export const metadata={title:'Healthcare Apply',description:'AI career agent for healthcare professionals'};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
