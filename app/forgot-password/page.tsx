'use client';import{FormEvent,useState}from'react';import{createClient}from'@/lib/supabase/client';export default function Forgot(){const[m,setM]=useState('');async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get('email'));const{error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/reset-password`});setM(error?error.message:'Check your email for a password reset link.')}return <main className="auth-shell"><section className="auth-card"><div className="brand">Healthcare Apply</div><h1>Reset password</h1><form onSubmit={go}><label>Email<input name="email" type="email" required/></label><button className="primary">Send reset link</button></form>{m&&<p className="notice">{m}</p>}<a href="/login">Back to sign in</a></section></main>}
