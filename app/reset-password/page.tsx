'use client';import{FormEvent,useState}from'react';import{createClient}from'@/lib/supabase/client';export default function Reset(){const[m,setM]=useState('');async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const p=String(new FormData(e.currentTarget).get('password'));const{error}=await createClient().auth.updateUser({password:p});setM(error?error.message:'Password updated. You can continue to your dashboard.')}return <main className="auth-shell"><section className="auth-card"><div className="brand">Healthcare Apply</div><h1>Choose a new password</h1><form onSubmit={go}><label>New password<input name="password" type="password" minLength={8} required/></label><button className="primary">Update password</button></form>{m&&<p className="notice">{m}</p>}<a href="/dashboard">Continue</a></section></main>}
